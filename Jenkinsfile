pipeline {
    agent any
    stages {
        stage('Pipeline Stages'){
            steps {
                sh "ls"
            }           
        }
        stage('second stage of pipeline t') {
            steps {
                sh "pwd"
            }
        }
     }
}
